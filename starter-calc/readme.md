I want a simple HTML page. JS and CSS embedded.
It is a calculator for how to feed starter.
It should be simple to use on mobile
I want to input: 
g per batch (default 100)
batches (default 1)
leftover (default 50)
above computes target total
optional override target total

Then I want to either set starter amount or feeding ratio. both should be blank. when you start typing in one field, the other automatically clears and becomes disabled until you erase the first. Whichever is filled drives the computation
ratio input should be a single number.

Output:
starter, water, flour, total grams
ratio (show whole numbers or up to 1 decimal)
